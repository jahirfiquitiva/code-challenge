const Intro = () => {
  return (<>
    <h1>Blog msco.</h1>
    <p>Explore the unknown. Uncover what matters. Prototype, test, repeat. Combine intuition with
      evidence. Design with intent and build it right.</p>
  </>);
};

export default Intro;
